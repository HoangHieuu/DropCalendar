# Create a dedicated Gemini authorization key and export it in your shell.
# Never commit a real value or put it in the macOS application.
GEMINI_API_KEY=
SNAPCAL_GEMINI_MODEL=gemini-2.5-flash
SNAPCAL_EXTRACTION_HOST=127.0.0.1
SNAPCAL_EXTRACTION_PORT=8765
